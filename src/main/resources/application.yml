spring:
  datasource:
    url: jdbc:mysql://localhost:3306/minha-vez_db?createDatabaseIfNotExist=true
    username: ${USUARIO_BD}
    password: ${SENHA_BD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
